# Dockerfiles-compile-scss-js

## Structure

### Gulp

The gulp setup is in the gulp folder and the next level is named by node version.

### Gulp compass

Same as gulp but with compass added to the image.
